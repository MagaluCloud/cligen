/*
*	DO NOT EDIT THIS FILE
*	IT IS AUTO GENERATED BY MGC
*	TEMPLATE: service_group.template
 */

package {{.PackageName}}

import (
	"context"

	{{range .Imports}}
	{{.}}
	{{end}}
)

func {{.FunctionName}}Cmd(ctx context.Context, parent *cobra.Command, {{.ServiceParam}}) {
	cmd := &cobra.Command{
		Use:     "{{.UseName}}",
		Short:   "{{.ShortDescription}}",
		Long:    `{{.LongDescription}}`,
		{{if .GroupID}}GroupID: "{{.GroupID}}",{{end}}
	}

	{{range .Commands}}
	{{.FunctionName}}(ctx, cmd, {{.ServiceCall}})
	{{end}}

	parent.AddCommand(cmd)
}
